# HISTORY

## 2026-04-10 (JST)

- 目的: 静的サイト本体と制作・保守用スクリプトを分離して管理しやすくする。
- 変更: 追跡されていた Python スクリプト（`*.py`）を `tools/python/` 配下へ集約（`git mv`）。
- 規模: `30` ファイル移動（すべてリネーム扱い / `R100`）。
- 影響: ページ表示時に Python が読み込まれることは想定していないため、サイトの動作には影響しないはず。

補足:

- 生成/整形系はファイルを書き換えるため、今後も「作業ブランチで実行→差分確認→反映」の手順を推奨。
